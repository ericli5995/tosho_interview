# Apache 2.4 virtual host for the container - the single source of Apache config.

<VirtualHost *:80>
    ServerName localhost

    # Only public/ is web-reachable; src/, config/, storage/ never are.
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        AllowOverride None
        Options +FollowSymLinks -Indexes
        Require all granted
        DirectoryIndex index.php

        # Front controller: real files/directories are served as-is,
        # everything else is handed to public/index.php.
        RewriteEngine On
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule ^ index.php [L]

        # Never serve dotfiles.
        <FilesMatch "^\.">
            Require all denied
        </FilesMatch>
    </Directory>

    # Uploaded images live in storage/uploads and are exposed through the
    # public/media symlink. Apache matches <Directory> on the resolved path,
    # so guard the real directory: static files only, never executed.
    <Directory /var/www/html/storage/uploads>
        Require all granted
        Options -ExecCGI -Indexes
        php_flag engine off
        RemoveHandler .php .phtml .phar
        RemoveType .php .phtml .phar
    </Directory>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>
