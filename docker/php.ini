; PHP overrides for the container.
upload_max_filesize = 8M
post_max_size       = 40M
memory_limit        = 256M
max_execution_time  = 60
date.timezone       = Asia/Tokyo
expose_php          = Off
; Session settings (save_path, gc, cookie flags) are set in src/Security/Session.php.
